SwitchMap:- (RxJS / Angular)

switchMap is an RxJS operator used to map values from one observable into a new observable, while cancelling the previous inner observable if a new value comes in.

👉 In simple words:
If a new value arrives → old request/work is cancelled → new one starts.
API calls based on user input (search box)

this.form.valueChanges.pipe
(debounceTime(200),
switchMap(value=>this.api.getUser(value))
).subscribe(result=>{
    this.user=result;
})

mergeMap:-Run all apis in Parallel.like i have list of order and i want this all details by id then use merge map:
Load multiple data at once
forms([1,2,3]).pipe(
    mergeMap(id=>this.userService.getuser(id)).subscribe(res=>this.user.push(res))
)

concateMap:-Save data in order -in queue
form([1,2,3]).pipe(
    concateMap(id=>this.service.getuser(id)).subscribe(res=>this.user.push(res))
)

exhaustMap:-Ignore New API Until Current Completes

fromEvent(this.loginBtn.nativeElement, 'click').pipe(
    exhaustMap(() => this.userService.getUser(1))
  ).subscribe(res => console.log(res));


forkJoin:-Waits for all APIs to finish.Then gives single combined result
forkJoin({
    users: this.api.getUsers(),
    orders: this.api.getOrders(),
    products: this.api.getProducts()
  }).subscribe(result => {

    console.log(result);

    // Access like this
    console.log(result.users);
    console.log(result.orders);
    console.log(result.products);

  });



Promise vs Observable:

Promise emiited single value like call api->response->Done
login Api File Upload One Time data-fetch
eager(runs immediately)
not cancelled
no operator

fetch('/api/user')
  .then(res => res.json())
  .then(data => console.log(data)); //Runs immediately. One result only.


  Observable:-Emitted multiple value over time:
  Lazy (runs only when subscribe)
  unsubscribes when needed
  operator:-map switchMap,debounce,retry

  this.http.getUser().subscribe(res=>res.data) //Runs only on subscribe. Can cancel.

Why Angular Prefers Observables:-
  Promise is good for single async operations, but Angular prefers Observables because they support streams, 
  cancellation, lazy execution, and powerful operators which are essential for reactive UI applications.
  1=>Reactive Architecture
  2=>Cancellation
  3=>Powefull Operator-switchmap
  4=>Prevent Memory Leakage.



APP_INITIALIZER function must return:

✅ Promise
OR
✅ Observable

runs config file before app start check auth environment must run promise and observable.

Webpack:-
Webpack is a module bundler used to bundle JavaScript, CSS, images, and other assets into optimized files for web apps.Angular 2+

esbuild:-bundler/transpiler --very fast Angular 17+
vite:-dev server+bundler ---HMR



angular.json is the main configuration file of an Angular workspace.
It controls how your Angular project is:

Built

Served

Tested

Generated

Deployed
define all app here in projects 

pack.json-configuration file manage serve build dependecies.
pack.lock.json auto generated file all installed version tree version here.


tsconfig.json is the TypeScript configuration file.
It tells TypeScript compiler:

How to compile code

Which files to include

Which JS version to generate

Which strict rules to apply


runtime.js

👉 Webpack / Build Runtime Loader

Contains

Module loading logic

Chunk loading mapping

Lazy loading handling

Dependency resolution at runtime


Analyze Bundle Size
ng build --stats-json


ng-container ----grouping html don't need extra tag not show in dom.
ng-template:-show in dom when condition true.
ng-content-show parent data in child if parent has used child selector./


Control Flow:-@if,@for,@switch,@defer

@defer-using for lazy load UI block,performance optimization

Hard to read

Extra <ng-template>

Question:-If I want to add data to an array without mutating the original array
Answer:-spread operator,concat,slice
this.item = this.item.concat(4);
this.item = [...this.item,4]
this.items = [...this.items.slice(), newItem];

Question:-Remove item without mutating array
this.items = this.items.filter(item => item.id !== id);
Question:-Update item in array (Immutable way)
users = [
 {id:1,name:'A'},
 {id:2,name:'B'}
];

this.users = this.users.map(user =>
  user.id === 2 ? {...user, name:'Updated'} : user
);

Here:

map() creates new array

Spread creates new object
Question:- ViewEncapsulation
Answer:-View Encapsulation in Angular is a feature that isolates component styles to prevent
them from affecting other components, and it supports Emulated, ShadowDom, and None modes.


Angular LifeCycle:-
ngOnChanges:-When parent and child relation and Parent send data to child
ngOnInIt:-Components Initialize One Time Run
ngDoCheck:-When Angular Chenge Detection
ngAfterContentInIt:-When ng-contetnt load in child component
ngAfterContentChecked:- When content Checked (check projected content changed through chnage detection)
ngAfterViewInit:-When Component View And child component Load
ngAfterViewChecked:- When Component View Checked
ngOnDestroy:- When You goto other Component route change

1.Emulated:- By default use global css.style scoped  
2.ShadowDom:-Component style isolate .does not affect gobal css
3.None:- make component style global

Question:-trackBy
Answer:-trackBy is a function used with ngFor to help Angular identify list items uniquely. 
By default, Angular tracks by object reference, which can cause full DOM re-render when array reference changes. Using trackBy with a unique identifier 
like ID ensures only changed items are updated, improving performance significantly in large lists.

Question:-Tree Shaking
Answer:-Tree shaking is a technique used by modern JavaScript bundlers 
to remove unused (dead) code from the final production bundle.

Question:Pipe
Answer:-A pipe transforms data in the template.
Angular has:

Built-in pipes

Custom pipes

Question:-Pure Pipe
Answer:-A pipe runs when input reference change
Question:Impure Pipe
Answer:-On Every change like event trigger,every async update


Subject:-Does not initial value .It does NOT store previous values.Subscribers receive only values emitted after they subscribe

Behaviour Subject:-it has initial value when subscribe get latest value.it stores last Emitted
value.

Replay Subject:-no required initial value.store previous value.buffer size add if 
we write 2 then every subsriber got last 2 value.

const subject = new ReplaySubject<any>(2);



subject.next(1);
subject.next(2);
subject.next(3);

subject.subscribe(v => console.log(v));[2,3]


Async Subject:-Emitted last value after Completes.Wait for result.

const subject = new AsyncSubject();

subject.subscribe(v => console.log(v));

subject.next(1);
subject.next(2);
subject.next(3);

subject.complete();


takeUntill():-An Angular with RxJS, takeUntil is an operator used to automatically unsubscribe from an Observable 
when another Observable emits a value.

It’s commonly used to prevent memory leaks in components.

private subject=new Subject<void>
this.userservice.getUser().pipe(takeUntil(this.subject)).subscribe(res=>res.json())


How do you handle error handling in RxJS streams? (catchError, retry, retryWhe  n)

share()=>.multicasting.don not share previous value. if we have 2 compoenent and i want to 1api call then we use it but data not shared with other component

const data$ = this.http.get('/api/users').pipe(
  share()
);

data$.subscribe();
data$.subscribe();

Only 1 HTTP call happens

shareReplay()=>if we have 2 compoenent and i want to 1api call then we use it and it store value and get data from cache.

const data$ = this.http.get('/api/users').pipe(
  shareReplay(1)
);

data$.subscribe();
data$.subscribe();

Only 1 HTTP call happens




JIT:-Compilation happen in browser at runtime
when user open app 
app loads
angular compiler run in browser
template get compiled

good for dev
easy debugging

AOT:-better for build
Compilation happen in build time
template get compiled at build time

when user open app everything compiled browser directly run js.

css-standard styling language .no need compilation browser understand directly
scss -browser compile we can define variable, nested style
sass-no need semicolon,curly braces. 

AppModule -> RouterModule.forRoot()  => sets main router

FeatureModule -> RouterModule.forChild() => adds child routes


take(1) emits the first value from an observable and then completes the subscription automatically.

of(1,2,3,4).pipe(
  take(1)
).subscribe(x => console.log(x)); //1



from(this.arr)
  .pipe(distinctUntilChanged())
  .subscribe(console.log);

  from(this.arr) → converts array to Observable

pipe() → apply RxJS operators

distinctUntilChanged() → remove consecutive duplicates

const arr = [1,1,2,2,3];

RxJS operators like distinctUntilChanged work only on Observables, 
so we must convert arrays using from() or of() before using them.

const arr = [
  { id: 1 },
  { id: 1 },
  { id: 2 },
  { id: 2 }
];

from(arr)
  .pipe(distinctUntilChanged())
  .subscribe(console.log); //1,1,2,2

  Because objects are compared by reference, not by value

  from(arr)
  .pipe(
    distinctUntilChanged((prev, curr) => prev.id === curr.id)
  )
  .subscribe(console.log); //{id:1}
{id:2}


from([1,2,1,3,2]).pipe(
  distinct()
).subscribe(console.log);


Dependency Injection hierarchy:-Root,Module,Component

Change Detection:-Default → checks entire component tree.
OnPush:-When input reference change and you can do manually like this.cdr.markForCheck
Used for performance optimization.


Lazy Load:-Fast initial load small bundle size.break in chunks
{
  path:'',
  loadChildren():=>import('').then(m=>m.DashboardModule)
}


Angular Performance Optimization:
OnPush
trackBy,async
takeUntill
Unsubscribe Observable,
Use Signal,
Lazy Loading
Pure Pipe,
Virtual Scroling

What are interceptors used for?
For auth logging
Error handling
Api transformation


Guards:
CanActivate
canLoad
CanDeactivate
Resolve

How do you manage state in Angular applications?
Services + BehaviorSubject
NGRX

Remove duplicates from array
const arr=[1,1,2,3,3,4];
const unique=[...new Set(arr)];
console.log(unique);

How do you handle memory leaks?
async pipe
takeUntil
unsubscribe in ngOnDestroy

const arr=[1,2,[3,4],[5,[6]]];
const result=arr.flat(Infinity);
console.log(result);

Debounce API call:-
this.form.get('name').valueChanges.pipe(debounceTime(200),distinctUntilChanged(),switchMap(val=>this.api.search(value))).subscribe();

destroy=new Subject<void>()
this.form.get('input').valueChanges.pipe(debounceTime(200),distinctUntilChanged(),tap(()=>this.loading=true).
switchMap(val=>this.api.search(val).pipe(catchError(()=>of([])))),
takeUntil(this.destroy)
).subscribe(res=>this.result=res;this.loading=false)

Large list (10k records) performance issue
Angular CDK Virtual Scroling
Lazy Load pagination

What is ESLint?:-
Code Analyzer For TypeScript and JavaScript

What is Generics?
Generics allow reusable components with type safety.
function identity<T>(value:T):T{
 return value
}

Accumulator generally means a variable that stores the running result while looping or processing data
let numbers = [1, 2, 3, 4];

let result = numbers.reduce((accumulator, currentValue) => {
  return accumulator + currentValue;
}, 0);

console.log(result);


What happens if you subscribe inside subscribe?
this.api.getUser().subscribe(user => {
  this.api.getOrders(user.id).subscribe(orders => {
    console.log(orders);
  });
});

Problem:-
Memory leak risk
Callback nesting
Hard to manage errors

Better Solution – use switchMap
this.api.getUser().pipe(switchMap(user=>this.api.getUserLIst(user.id))).subscribe(order=>console.log(order))

Interface:-An interface defines the structure of an object.
type can define objects, unions, primitives, functions,
enum is used to define a set of named constants.

@ViewChild is used when you want to get a single element or component from the view.
@ViewChild('nameInput') input!: ElementRef;

  ngAfterViewInit() {
    console.log(this.input.nativeElement.value);
  }

  @ViewChildren is used when there are multiple elements or components.
  @ViewChildren('item') items!: QueryList<ElementRef>;

  ngAfterViewInit() {
    this.items.forEach(el => {
      console.log(el.nativeElement.textContent);
    });
  }

  @ContentChild
  <app-card>
  <p #contentText>Hello World</p>
</app-card>
<div class="card">
  <ng-content></ng-content>
</div>
 @ContentChild('contentText') text!: ElementRef;

  ngAfterContentInit() {
    console.log(this.text.nativeElement.textContent);
  }

@ContentChildren:-Used when multiple projected elements exist.
<app-card>
  <p #item>Item 1</p>
  <p #item>Item 2</p>
  <p #item>Item 3</p>
</app-card>

custome Directive:-
this.el.nativeElement.style.color = 'red';

Renderer2:-Renderer2 is a service used to safely manipulate the DOM
such as styles, attributes, classes, and events without directly accessing the native DOM.
this.renderer.setStyle(this.el.nativeElement, 'color', 'blue');


@HostListener in Angular is a decorator used to listen to DOM events on the host element


Question:-take(1)
take first emiited value and unsubscribe automatically
source$.pipe(
  take(1)
).subscribe({
  next: val => console.log('take(1):', val),
  complete: () => console.log('take(1) completed')
});

Question:-first()
Answer:-same as take(1).mostly used for condition throw error if no value
source$.pipe(
  first(val => val > 2)
).subscribe({
  next: val => console.log('first > 2:', val)
});


Question:-Smart & Dumb Component
Answer:-Smart components handle business logic and API calls,State while dumb components are 
purely presentational and receive data via inputs and emit events via outputs,Display Data.

Question:What are the main components of NgRx?
Answer:Store:-Hold state
Action:- Event
Reducers:-Update State
Selector:-Get State
Effect:-Api call handle

Component dispatches an action → Effect calls API → Reducer updates store → Component gets data via selector.

Question:Custome Pipe
Answer:-@Pipe({
  name:'capitalize'
})
export class Capital implements PipeTransform{

  transform(value:string):string{
if(!value) return '';
return value.charAt(0).toUpperCase() + value.slice(1);
  }
}
<p>{{'danish' | capitalize }}</p>


Question:-Custome Directive
Answer:
@Directive({
  name:'[appHighlight]'
})
export class Custome{
  constructor(private renderer:Renderer2,private el:ElementRef){

  }
  @HostListener('mouseenter')
  this.renderer.setStyle(this.el.nativeElement,'backgroundColor','red')
}
<p appHighlight>Click</p>

Question:What happens if you don’t call next.handle()?
Answer:-Request will never reach API

Question:Explain interceptor params
Answer:-intercept method takes HttpRequest and HttpHandler. HttpRequest contains request details and is immutable, 
so we clone it to modify. HttpHandler is used to forward the request to the next interceptor or backend.

Question:-What is an Interceptor in Angular
Answer:-Interceptor is used to modify or handle HTTP requests and responses globally.

Question:-next.handle() is Mandatory
Answer:-without next,handle request will not go to API

Question:-ngClass and ngStyle
Answer:-
ngStyle is inline style ngClass is used add/remove CSS classes dynamically

Question:-How to add remove class dynamically
Answer:-
addClass() {
  this.renderer.addClass(this.el.nativeElement, 'active');
}

removeClass() {
  this.renderer.removeClass(this.el.nativeElement, 'active');
}
